pipeline {
    agent any
parameters {
  choice choices: ['develope'], description: 'this is dev', name: 'dev'
}
    stages {
        stage('Hello') {
            steps {
                echo 'Hello World'
            }
        }
    }
}
